<!-- page.ejs -->

<h1><%= page.charAt(0).toUpperCase() + page.slice(1) %></h1>

<h2><%= page.charAt(0).toUpperCase() + page.slice(1) %> Posts</h2>

<% posts.forEach(function(post) { %>
  <h3><%= post.title %></h3>
  <p><%= post.content %></p>
<% }); %>
